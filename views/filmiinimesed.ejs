<!DOCTYPE html>
<html lang="et">
<head>
  <meta charset="utf-8">
  <title>Filmiinimesed</title>
</head>
<body>
  <h1>Filmiinimesed</h1>
  <p><a href="/eestifilm">← Tagasi filmide juurde</a></p>

  <% if (people && people.length > 0) { %>
    <ul>
      <% people.forEach(person => { %>
        <li><strong><%= person.eesnimi %> <%= person.perenimi %></strong> — <%= person.amet %></li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>Filmiinimesi pole veel lisatud.</p>
  <% } %>

  <hr>
  <p><a href="/filmiinimesed/add">Lisa uus filmiinimene</a></p>
</body>
</html>
